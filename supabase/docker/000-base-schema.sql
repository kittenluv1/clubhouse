-- Base schema placeholder.
-- Replace this file with a dump of your hosted Supabase public schema:
--
--   pg_dump \
--     --schema-only --no-owner --no-privileges \
--     -n public \
--     "YOUR_SUPABASE_CONNECTION_STRING" \
--     > supabase/docker/000-base-schema.sql
--
-- Required before running: docker compose --env-file .env.docker up
